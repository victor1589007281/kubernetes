# 解题技巧与题目变种

## 目录
1. [思维技巧](#思维技巧)
2. [题目变种分析](#题目变种分析)
3. [解题模板总结](#解题模板总结)
4. [常见陷阱与边界](#常见陷阱与边界)
5. [复杂度分析技巧](#复杂度分析技巧)

---

## 思维技巧

### 正难则反

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            正难则反思想                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   当正向思考困难时，尝试从反面思考                                           │
│                                                                              │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  例1: 找最长不包含X的子串                                        │       │
│   │       ↓ 转化                                                     │       │
│   │       找所有包含X的位置，用它们分割字符串                        │       │
│   │                                                                  │       │
│   │  例2: 除自身以外数组的乘积 (不能用除法)                          │       │
│   │       ↓ 转化                                                     │       │
│   │       左边乘积 × 右边乘积                                        │       │
│   │                                                                  │       │
│   │  例3: 接雨水 (求每个位置能存多少水)                              │       │
│   │       ↓ 转化                                                     │       │
│   │       每个位置的水量 = min(左边最高, 右边最高) - 当前高度        │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 问题转化

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            问题转化技巧                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   1. 和问题 ↔ 差问题                                                        │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  两数之和 = target  ↔  num2 = target - num1                     │       │
│   │  和为K的子数组     ↔  prefix[j] - prefix[i] = K                 │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│   2. 最值问题 ↔ 判定问题                                                    │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  最大化最小值 → 二分 + 判定是否可行                              │       │
│   │  最小化最大值 → 二分 + 判定是否可行                              │       │
│   │                                                                  │       │
│   │  例: 分割数组的最大值 (410)                                      │       │
│   │      二分答案，判断能否在m次分割内使每组和不超过mid              │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│   3. 计数问题 ↔ 动态规划                                                    │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  求方案数 → 定义状态 dp[i] = 到达状态i的方案数                  │       │
│   │  状态转移 → dp[i] += dp[j] (从j可以到达i)                       │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│   4. 子数组问题 ↔ 前缀和/差分                                               │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  区间求和 → 前缀和                                               │       │
│   │  区间修改 → 差分数组                                             │       │
│   │  连续子数组 → 滑动窗口                                           │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 空间换时间

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            空间换时间                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  技巧                    典型应用                                │       │
│   │  ─────────────────────   ─────────────────────────              │       │
│   │  哈希表加速查找          两数之和 O(n) vs O(n²)                 │       │
│   │  前缀和预处理            区间和查询 O(1) vs O(n)                │       │
│   │  记忆化搜索              斐波那契 O(n) vs O(2^n)                │       │
│   │  预处理左右最值          接雨水 O(n) vs O(n²)                   │       │
│   │  邻接表存图              图遍历 O(V+E) vs O(V²)                 │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 题目变种分析

### 两数之和系列

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          两数之和变种                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   基础题: 1. 两数之和 (无序数组)                                             │
│   解法: 哈希表 O(n)                                                          │
│                                                                              │
│   变种1: 167. 两数之和 II (有序数组)                                         │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  解法: 双指针                                                    │       │
│   │  left, right := 0, n-1                                          │       │
│   │  sum < target → left++                                          │       │
│   │  sum > target → right--                                         │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│   变种2: 15. 三数之和                                                        │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  解法: 排序 + 固定一个 + 双指针                                  │       │
│   │  注意: 去重处理                                                  │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│   变种3: 18. 四数之和                                                        │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  解法: 排序 + 固定两个 + 双指针                                  │       │
│   │  优化: 剪枝 (最小和 > target 或 最大和 < target)                 │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│   变种4: 653. 两数之和 IV - BST                                              │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  解法1: 中序遍历转数组 + 双指针                                  │       │
│   │  解法2: DFS + 哈希表                                             │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│   变种5: 560. 和为K的子数组                                                  │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  解法: 前缀和 + 哈希表                                           │       │
│   │  prefix[j] - prefix[i] = K → 查找 prefix[i] = prefix[j] - K     │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 链表反转系列

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          链表反转变种                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   基础题: 206. 反转链表                                                      │
│                                                                              │
│   变种1: 92. 反转链表 II (区间反转)                                          │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  1 → 2 → 3 → 4 → 5, left=2, right=4                             │       │
│   │  1 → 4 → 3 → 2 → 5                                              │       │
│   │                                                                  │       │
│   │  步骤:                                                           │       │
│   │  1. 找到 left-1 位置 (prev)                                     │       │
│   │  2. 反转 [left, right] 区间                                     │       │
│   │  3. 连接 prev 和反转后的头                                       │       │
│   │  4. 连接反转后的尾和 right+1                                     │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│   变种2: 25. K个一组翻转链表                                                 │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  1 → 2 → 3 → 4 → 5, k=2                                         │       │
│   │  2 → 1 → 4 → 3 → 5                                              │       │
│   │                                                                  │       │
│   │  步骤:                                                           │       │
│   │  1. 检查剩余节点数 >= k                                          │       │
│   │  2. 反转 k 个节点                                                │       │
│   │  3. 递归处理剩余链表                                             │       │
│   │  4. 连接                                                         │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│   变种3: 24. 两两交换链表中的节点                                            │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  1 → 2 → 3 → 4                                                  │       │
│   │  2 → 1 → 4 → 3                                                  │       │
│   │  特例: K=2 的翻转                                                │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│   变种4: 234. 回文链表                                                       │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  步骤:                                                           │       │
│   │  1. 快慢指针找中点                                               │       │
│   │  2. 反转后半部分                                                 │       │
│   │  3. 比较前后两部分                                               │       │
│   │  4. (可选) 恢复链表                                              │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 子数组/子串问题

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        子数组/子串问题分类                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  问题类型                解法                    典型题目        │       │
│   │  ────────────────────    ────────────────────    ────────────── │       │
│   │  最长/最短连续子数组      滑动窗口                3, 76, 209     │       │
│   │  子数组和等于K           前缀和+哈希              560, 523       │       │
│   │  最大子数组和            DP/分治                  53             │       │
│   │  乘积最大子数组          DP (记录最大最小)        152            │       │
│   │  子数组最值              单调栈/单调队列          239, 739       │       │
│   │  子数组计数              DP/双指针                795, 907       │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│   滑动窗口适用条件:                                                          │
│   1. 求连续子数组/子串                                                       │
│   2. 窗口满足单调性 (扩大窗口不会使不满足变满足，或反之)                     │
│                                                                              │
│   前缀和适用条件:                                                            │
│   1. 子数组和相关问题                                                        │
│   2. 需要 O(1) 计算区间和                                                    │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 二叉树问题

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        二叉树问题分类                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  问题类型                解法                    典型题目        │       │
│   │  ────────────────────    ────────────────────    ────────────── │       │
│   │  遍历类                  DFS递归/迭代            94, 144, 145   │       │
│   │  层序遍历                BFS队列                 102, 107, 199  │       │
│   │  树的属性                递归                    104, 101, 110  │       │
│   │  路径问题                DFS+回溯                112, 113, 437  │       │
│   │  构造树                  递归分治                105, 106, 654  │       │
│   │  BST问题                 中序遍历                98, 230, 538   │       │
│   │  公共祖先                递归                    236, 235       │       │
│   │  序列化                  BFS/DFS                 297            │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│   递归思路:                                                                  │
│   1. 这个节点要做什么？                                                      │
│   2. 什么时候做 (前序/中序/后序位置)？                                       │
│   3. 递归调用左右子树                                                        │
│                                                                              │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  前序位置: 进入节点时处理，适合自顶向下传递信息                  │       │
│   │  后序位置: 离开节点时处理，适合收集子树信息                      │       │
│   │  中序位置: BST中产生有序序列                                     │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 背包问题变种

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          背包问题变种                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  类型          特点                    典型题目                   │       │
│   │  ──────────    ────────────────────    ────────────────────────  │       │
│   │  01背包        每个物品最多选1次        416 分割等和子集          │       │
│   │  完全背包      每个物品可选无限次       322 零钱兑换              │       │
│   │  多重背包      每个物品有数量限制       -                         │       │
│   │  分组背包      每组最多选1个            -                         │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│   01背包模板 (一维优化):                                                     │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  for i := 0; i < n; i++ {                                       │       │
│   │      for w := W; w >= weight[i]; w-- {  // 倒序!                │       │
│   │          dp[w] = max(dp[w], dp[w-weight[i]] + value[i])         │       │
│   │      }                                                          │       │
│   │  }                                                              │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│   完全背包模板 (一维优化):                                                   │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  for i := 0; i < n; i++ {                                       │       │
│   │      for w := weight[i]; w <= W; w++ { // 正序!                 │       │
│   │          dp[w] = max(dp[w], dp[w-weight[i]] + value[i])         │       │
│   │      }                                                          │       │
│   │  }                                                              │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│   问题变形:                                                                  │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  • 求最大价值 → 标准背包                                        │       │
│   │  • 求恰好装满 → 初始化 dp[0]=0, 其他=无穷小                     │       │
│   │  • 求方案数   → dp[w] += dp[w-weight[i]]                        │       │
│   │  • 求最少个数 → dp[w] = min(dp[w], dp[w-weight[i]] + 1)         │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 解题模板总结

### 二分查找模板

```go
// 模板1: 标准二分 (找确切值)
func binarySearch(nums []int, target int) int {
    left, right := 0, len(nums)-1
    for left <= right {
        mid := left + (right-left)/2
        if nums[mid] == target {
            return mid
        } else if nums[mid] < target {
            left = mid + 1
        } else {
            right = mid - 1
        }
    }
    return -1
}

// 模板2: 左边界 (第一个 >= target)
func lowerBound(nums []int, target int) int {
    left, right := 0, len(nums)
    for left < right {
        mid := left + (right-left)/2
        if nums[mid] >= target {
            right = mid
        } else {
            left = mid + 1
        }
    }
    return left
}

// 模板3: 右边界 (最后一个 <= target)
func upperBound(nums []int, target int) int {
    left, right := 0, len(nums)
    for left < right {
        mid := left + (right-left)/2
        if nums[mid] > target {
            right = mid
        } else {
            left = mid + 1
        }
    }
    return left - 1
}
```

### 回溯模板

```go
// 排列模板 (顺序相关)
func permute(nums []int) [][]int {
    result := [][]int{}
    used := make([]bool, len(nums))
    
    var backtrack func(path []int)
    backtrack = func(path []int) {
        if len(path) == len(nums) {
            result = append(result, append([]int{}, path...))
            return
        }
        for i := 0; i < len(nums); i++ {
            if used[i] {
                continue
            }
            used[i] = true
            path = append(path, nums[i])
            backtrack(path)
            path = path[:len(path)-1]
            used[i] = false
        }
    }
    
    backtrack([]int{})
    return result
}

// 组合模板 (顺序无关)
func combine(n int, k int) [][]int {
    result := [][]int{}
    
    var backtrack func(start int, path []int)
    backtrack = func(start int, path []int) {
        if len(path) == k {
            result = append(result, append([]int{}, path...))
            return
        }
        // 剪枝: 剩余元素不够
        for i := start; i <= n-(k-len(path))+1; i++ {
            path = append(path, i)
            backtrack(i+1, path)
            path = path[:len(path)-1]
        }
    }
    
    backtrack(1, []int{})
    return result
}

// 子集模板
func subsets(nums []int) [][]int {
    result := [][]int{}
    
    var backtrack func(start int, path []int)
    backtrack = func(start int, path []int) {
        result = append(result, append([]int{}, path...))
        for i := start; i < len(nums); i++ {
            path = append(path, nums[i])
            backtrack(i+1, path)
            path = path[:len(path)-1]
        }
    }
    
    backtrack(0, []int{})
    return result
}
```

### BFS模板

```go
// 层序遍历模板
func levelOrder(root *TreeNode) [][]int {
    if root == nil {
        return nil
    }
    
    result := [][]int{}
    queue := []*TreeNode{root}
    
    for len(queue) > 0 {
        levelSize := len(queue)
        level := []int{}
        
        for i := 0; i < levelSize; i++ {
            node := queue[0]
            queue = queue[1:]
            level = append(level, node.Val)
            
            if node.Left != nil {
                queue = append(queue, node.Left)
            }
            if node.Right != nil {
                queue = append(queue, node.Right)
            }
        }
        result = append(result, level)
    }
    return result
}

// 最短路径模板 (图/矩阵)
func shortestPath(grid [][]int) int {
    m, n := len(grid), len(grid[0])
    if grid[0][0] == 1 || grid[m-1][n-1] == 1 {
        return -1
    }
    
    dirs := [][]int{{0,1}, {0,-1}, {1,0}, {-1,0}}
    visited := make([][]bool, m)
    for i := range visited {
        visited[i] = make([]bool, n)
    }
    
    queue := [][]int{{0, 0}}
    visited[0][0] = true
    steps := 0
    
    for len(queue) > 0 {
        size := len(queue)
        for i := 0; i < size; i++ {
            cur := queue[0]
            queue = queue[1:]
            
            if cur[0] == m-1 && cur[1] == n-1 {
                return steps
            }
            
            for _, d := range dirs {
                nx, ny := cur[0]+d[0], cur[1]+d[1]
                if nx >= 0 && nx < m && ny >= 0 && ny < n &&
                   !visited[nx][ny] && grid[nx][ny] == 0 {
                    visited[nx][ny] = true
                    queue = append(queue, []int{nx, ny})
                }
            }
        }
        steps++
    }
    return -1
}
```

### 并查集模板

```go
type UnionFind struct {
    parent []int
    rank   []int
}

func NewUnionFind(n int) *UnionFind {
    parent := make([]int, n)
    rank := make([]int, n)
    for i := range parent {
        parent[i] = i
    }
    return &UnionFind{parent, rank}
}

func (uf *UnionFind) Find(x int) int {
    if uf.parent[x] != x {
        uf.parent[x] = uf.Find(uf.parent[x]) // 路径压缩
    }
    return uf.parent[x]
}

func (uf *UnionFind) Union(x, y int) bool {
    rootX, rootY := uf.Find(x), uf.Find(y)
    if rootX == rootY {
        return false
    }
    // 按秩合并
    if uf.rank[rootX] < uf.rank[rootY] {
        uf.parent[rootX] = rootY
    } else if uf.rank[rootX] > uf.rank[rootY] {
        uf.parent[rootY] = rootX
    } else {
        uf.parent[rootY] = rootX
        uf.rank[rootX]++
    }
    return true
}

func (uf *UnionFind) Connected(x, y int) bool {
    return uf.Find(x) == uf.Find(y)
}
```

---

## 常见陷阱与边界

### 数组边界

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          常见边界问题                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   1. 空数组/空字符串                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  if len(nums) == 0 { return ... }                               │       │
│   │  if s == "" { return ... }                                      │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│   2. 单元素                                                                  │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  if len(nums) == 1 { return nums[0] }                           │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│   3. 索引越界                                                                │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  i-1 >= 0 && i+1 < n  // 访问相邻元素前检查                     │       │
│   │  nums[i%n]            // 循环数组                               │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│   4. 整数溢出                                                                │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  mid := left + (right-left)/2  // 避免 (left+right)/2 溢出     │       │
│   │  使用 int64 处理大数乘法                                        │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│   5. 负数处理                                                                │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  -7 % 3 = -1 (Go)  // 注意负数取模                              │       │
│   │  (n%m + m) % m     // 确保结果为正                              │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 链表陷阱

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          链表常见陷阱                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   1. 修改指针前保存 next                                                     │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  next := curr.Next  // 先保存                                   │       │
│   │  curr.Next = prev   // 再修改                                   │       │
│   │  curr = next        // 最后移动                                 │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│   2. 使用 dummy 节点简化头节点处理                                           │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  dummy := &ListNode{Next: head}                                 │       │
│   │  // 处理...                                                     │       │
│   │  return dummy.Next                                              │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│   3. 快慢指针找中点的边界                                                    │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  偶数节点: slow 停在前半部分末尾还是后半部分开头？              │       │
│   │  for fast != nil && fast.Next != nil  // slow在左中点           │       │
│   │  for fast.Next != nil && fast.Next.Next != nil // slow在右中点  │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 复杂度分析技巧

### 时间复杂度速查

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          常见复杂度                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  复杂度            典型操作                   n=10⁶时间          │       │
│   │  ──────────────    ────────────────────────   ─────────────     │       │
│   │  O(1)              哈希查找、数组访问          1 微秒            │       │
│   │  O(log n)          二分查找                    20 微秒           │       │
│   │  O(n)              线性遍历                    1 毫秒            │       │
│   │  O(n log n)        排序                        20 毫秒           │       │
│   │  O(n²)             双重循环                    15 分钟           │       │
│   │  O(n³)             三重循环                    30 年             │       │
│   │  O(2ⁿ)             子集枚举                    n=20就极限        │       │
│   │  O(n!)             全排列                      n=10就极限        │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│   根据 n 的范围选择算法:                                                     │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  n ≤ 10        O(n!)         全排列                             │       │
│   │  n ≤ 20        O(2ⁿ)         子集、状态压缩DP                   │       │
│   │  n ≤ 500       O(n³)         区间DP、Floyd                      │       │
│   │  n ≤ 5000      O(n²)         简单DP、暴力                       │       │
│   │  n ≤ 10⁶       O(n log n)    排序、堆                           │       │
│   │  n ≤ 10⁷       O(n)          线性遍历                           │       │
│   │  n > 10⁷       O(log n)      二分、数学                         │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 递归复杂度分析

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          递归复杂度                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   主定理 (Master Theorem):                                                   │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  T(n) = a·T(n/b) + O(nᵈ)                                        │       │
│   │                                                                  │       │
│   │  若 d > log_b(a): T(n) = O(nᵈ)                                  │       │
│   │  若 d = log_b(a): T(n) = O(nᵈ log n)                            │       │
│   │  若 d < log_b(a): T(n) = O(n^(log_b(a)))                        │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│   常见递归:                                                                  │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  二分查找:     T(n) = T(n/2) + O(1) = O(log n)                  │       │
│   │  归并排序:     T(n) = 2T(n/2) + O(n) = O(n log n)               │       │
│   │  快速排序:     平均 O(n log n), 最坏 O(n²)                      │       │
│   │  斐波那契:     T(n) = T(n-1) + T(n-2) = O(2ⁿ)                   │       │
│   │  带记忆化:     O(n)                                              │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 高频面试题 Top 20

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          高频面试题                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  排名   题号   题目                          核心技巧            │       │
│   │  ─────  ─────  ────────────────────────────  ─────────────────  │       │
│   │  1      1      两数之和                      哈希表              │       │
│   │  2      206    反转链表                      双指针/递归         │       │
│   │  3      3      无重复字符的最长子串          滑动窗口            │       │
│   │  4      146    LRU缓存                       哈希+双向链表       │       │
│   │  5      215    第K大元素                     快速选择/堆         │       │
│   │  6      53     最大子数组和                  DP/分治             │       │
│   │  7      21     合并两个有序链表              递归/迭代           │       │
│   │  8      102    二叉树的层序遍历              BFS                 │       │
│   │  9      15     三数之和                      排序+双指针         │       │
│   │  10     121    买卖股票的最佳时机            DP/贪心             │       │
│   │  11     200    岛屿数量                      DFS/BFS             │       │
│   │  12     20     有效的括号                    栈                  │       │
│   │  13     141    环形链表                      快慢指针            │       │
│   │  14     236    二叉树的最近公共祖先          递归                │       │
│   │  15     33     搜索旋转排序数组              二分查找            │       │
│   │  16     46     全排列                        回溯                │       │
│   │  17     70     爬楼梯                        DP                  │       │
│   │  18     300    最长递增子序列                DP/二分             │       │
│   │  19     42     接雨水                        双指针/单调栈       │       │
│   │  20     322    零钱兑换                      完全背包DP          │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

